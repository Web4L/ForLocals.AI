# Review Requests

Generate review-request campaigns over SMS and email — with direct review links and polite
follow-ups — to steadily grow Google/Yelp ratings.

**Reads from brand context:** `brand`, `voice`.
**Status:** planned (scaffold only).

- `prompts/` — request-message and sequence templates.
- `src/` — optional code (e.g. SMS/email send), unused for now.
