# SEO Blog & Landing Pages

Generate SEO-optimized blog posts and local/service landing pages in the business's voice —
with meta titles, descriptions, and outlines.

**Reads from brand context:** `brand`, `voice`, `key-messaging`.
**Status:** planned (scaffold only).

- `prompts/` — blog/SEO prompt templates.
- `src/` — optional code (e.g. schema/SEO tooling), unused for now.
