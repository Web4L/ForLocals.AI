# Support & FAQ

Draft customer support replies and build FAQ / chatbot answer sets in the business's voice,
so common questions get fast, on-brand responses.

**Reads from brand context:** `brand`, `voice`.
**Status:** planned (scaffold only).

- `prompts/` — support reply and FAQ templates.
- `src/` — optional code (e.g. chatbot integration), unused for now.
