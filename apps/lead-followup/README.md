# Lead Follow-Up

Build automated lead nurture and follow-up sequences over SMS and email that move new
leads toward the primary offer without being pushy.

**Reads from brand context:** `brand`, `voice`, `primaryOffer`.
**Status:** planned (scaffold only).

- `prompts/` — nurture sequence templates.
- `src/` — optional code (e.g. SMS/email automation), unused for now.
