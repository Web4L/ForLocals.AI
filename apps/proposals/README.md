# Proposals & Quotes

Generate proposals, quotes, and estimates in the business's voice and branding — turning a
scope of work into a clear, persuasive document.

**Reads from brand context:** `brand`, `voice`, `primaryOffer`.
**Status:** planned (scaffold only).

- `prompts/` — proposal/quote templates.
- `src/` — optional code (e.g. PDF generation), unused for now.
